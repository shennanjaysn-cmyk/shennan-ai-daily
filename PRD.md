# 产品需求文档（PRD）· 深南AI日报 Daily AI Brief

- 文档版本：v1.1
- 对应产品版本：v1.1.2（2026-07-25）
- 状态：视觉与品牌定版，待补合规收尾与分类多元化

---

## 1. 背景与目标

AI 圈信息密度高、来源分散，需要一份**每日可快速扫读、可一键溯源**的精选简报。本项目把 AIHOT 公开接口的每日精选，沉淀成固定版式的单页仪表盘，部署至 GitHub Pages。

目标不是做内容，而是做**可信的索引层**：标题 + 摘要 + 原文链接，不搬运、不二创。

## 2. 目标用户

- 主：AI 从业者、产品 / 设计向的每日扫读用户
- 次：开源后访问站点的普通 AI 关注者
- 非目标：需要深度分析、研报级内容的研究者

## 3. 核心功能

| 编号 | 功能 | 说明 | 状态 |
|---|---|---|---|
| F1 | 每日自动拉取 | 调 AIHOT `/api/public/daily`，失败回退归档接口 | ✅ |
| F2 | 单页仪表盘生成 | 5 版块网格、全局连续编号、锚点导航 | ✅ |
| F3 | 品牌化视觉 | 暗色 + 群青 + 金 / 薰衣草；SN_logo 左上角 | ✅ |
| F4 | Hover 推下式展开 | 卡片真实变高，下方同步下移，左右不动 | ✅ |
| F5 | 弹性阻尼动效 | 展开回弹、收起缓落，节奏差明显 | ✅ |
| F6 | 海外源提示 | 「直达原文。」后 [?] 图标，hover 提示网络环境 | ✅ |
| F7 | 版本化归档 | 每次产出 `ai-daily-vX.Y.Z_YYMMDD.html` | ✅ |
| F8 | 分类多元化 | KOL / 具身 / 大会等动态槽位 | ❌ 规划中（v1.2.0） |

## 4. 版块分类规则

数据源当前返回 **5 个固定桶**，映射策略如下：

- **三大常客**（永远置顶、高权重、可多内容）：
  - `模型发布/更新` → 显示「模型·发布与更新」
  - `产品发布/更新` → 显示「产品·发布与更新」
  - `行业动态` → 显示「行业·市场动态」
- **灵活槽位**（按 API 返回顺序紧随，随数据源扩展）：
  - `论文研究` → 显示「论文·研究速递」
  - `技巧与观点` → 显示「观点·技巧与洞察」

> 显示名与排序只在生成器层做映射，**数据源的原始 label 桶不变**（见 `generate_dashboard.py` 的 `SECTION_PRIORITY` / `SECTION_DISPLAY`）。

## 5. 视觉规范

### 5.1 配色

- 主背景 `#0A0E1A`；卡片底 `#11162A` 系；hero 装饰金 `#DAC887`
- 品牌金（Brief）`#DDD090`；品牌薰衣草（深南AI日报）`#cdc8ff`
- 卡片 hover 金边 `#837E65` / 2px；按钮 `#4F5CC7`

### 5.2 字体（SIL OFL 开源授权，允许子集化 + 再分发，已子集化 woff2）

- 中文标题 / 卡片标题：ZCOOL XiaoWei；中文正文：系统 CJK 回退栈（不嵌入）
- 英文 / 数字：Space Grotesk（可变 300–700）；英文正文：Poppins
- 早期 SmileySans / MiSans VF / Neue Regrade 因许可禁止子集化（二改），已于 v1.8.0 全面替换为 OFL 字体
- 规则：一种语言占 display，另一种只用 base 字体；数字默认不用特殊字体（避免「15」被当成装饰）

### 5.3 版式

- Hero：logo + 字标 / 标题（Daily AI 白 + Brief 金）/ 副标题（深南AI日报 薰衣草 + Daily AI 白）/ 送达时间 / 引用块覆盖窗口 / 统计格
- 版块头：罗马数字（按实际位置动态分配）+ 版块名 + 计数
- 卡片：序号 + 标题（≤32 字截断）+ 来源 chip + ≤60 字摘要 + 原文跳转（`target="_blank" rel="noopener noreferrer"`）
- 页脚：本期共 → 数据源 → 3px 粗金线 → Designed by Jaysn → Copyright → 文档链接（LICENSE / 免责声明 / 仓库 / README，带 icon）
- 移动端：刘海安全区适配

## 6. 交互规范

- **Hover 推下式**：`.card-grid { align-items: start }` + 卡片 `max-height` 真实展开；hover 时长高，下方随文档流下移，**左右同行不动**（摒弃 scale 覆盖式，避免 hover 闪烁与邻居抖动）。
- **弹性阻尼**：展开 `cubic-bezier(.34, 1.5, .64, 1)` .62s（轻微回弹）；收起标准 ease .84s（比展开慢一倍，缓落）。
- **海外源 [?]**：小圆圈问号图标，hover 显示「部分海外源链接需特殊网络环境访问」。
- 入场：`opacity` + `transform` 用 transition（**禁用 `animation ... both`**，会锁死 transform 破坏 hover）。

## 7. 交付物与部署

- 单页 HTML（外链 woff2，~39KB）+ 字体目录 + logo 位图
- 主线部署：**GitHub Pages**，源 = `releases/`（`index.html` 恒定；`deploy.yml` 的 `publish_dir: ./releases`）

## 8. 非功能需求

- **合规**：开源、非盈利；仅聚合标题 / 摘要 / 链接；第三方商标归属原权利人；提供侵权联系通道；不构成投资建议
- **性能**：HTML 轻量外链字体，避免内嵌膨胀
- **可维护**：版本号语义化 + 日期后缀；生成器单文件职责清晰

## 9. 范围外（明确不做）

- 不存储 / 改写原文全文（仅摘要 ≤60 字）
- 不做付费 / 广告 / 会员（保持非盈利抗辩）
- 不抓取多源做全文聚合（当前单源 AIHOT）
- 不自动重分类为 KOL / 具身等细分（v1.1.0 起规划）
